Application basics

The program is a simple console-executable, that:
    Loads an image
    Translates the image into ASCII art
    Applies filters(Brightness,InvertImage, flip)  if required – no filters by default
    Outputs the image into an output (console, file, …)
